# 1. 📝 앙상블 계획서: 자율주행 객체 탐지 성능 향상

### “피플넷(PeopleNet), 트라픽넷(TrafficNet), YOLOv11”


### 1️⃣ **프로젝트 개요**
- **목적:**  
  자율주행 환경에서 다양한 상황(보행자·차량·교통 이벤트 등)을 신뢰성 있게 탐지하기 위해 PeopleNet, TrafficNet, YOLOv11 세 가지 모델을 앙상블하여 단일 모델 대비 성능을 개선한다.
- **적용 분야:**  
  실시간 자율주행 비전 시스템(차량, CCTV 등)

### 2️⃣ **사용 모델 및 특성**
| 모델명      | 강점/특화 영역                                         |
|-------------|------------------------------------------------------|
| PeopleNet   | 사람, 가방, 얼굴 등 인체 감지에 특화                   |
| TrafficNet  | 교통상황 분류, 차량·사고·화재 등 교통 이벤트 탐지    |
| YOLOv11     | 다양한 객체의 고속·고정밀 실시간 탐지                |

### 3️⃣ **데이터셋 선정 및 준비**
- **데이터:** 자율주행 오픈데이터셋 (예: BDD100K, TuSimple, Cityscapes 등)
- **전처리:** 모든 모델이 호환 가능한 입력 해상도·포맷으로 변환

### 4️⃣ **진행 절차**

1. **개별 모델 성능 평가**
   - 동일 입력에 대해 세 모델 각각 추론
   - 각 모델별 객체(클래스, 위치, 신뢰도) 결과 기록

2. **앙상블 방법 설계・구현**
   - **앙상블 방식 1: 소프트 보팅 (confidence 평균)**
     - 세 모델 결과에서 동일 객체가 2개 이상 검출되면 confidence 평균
     - 임계값(예: 0.5) 이상만 최종 검출로 인정
   - **앙상블 방식 2: 하드 보팅 (다수결)**
     - **세 모델 중 “2개 이상”이 같은 객체(같은 위치·종류)를 잡으면 그것만 최종 결과로 채택**
     - 예를 들어, 어떤 객체를 PeopleNet과 YOLOv11 둘 다 발견했다면, 그 객체는 “최종 결과”로 인정  
     - 단, 한 모델만 잡은(나머지 모델은 못 찾은) 객체는 최종 결과에 포함되지 않음  
     - 이 방식의 의미: 여러 모델이 동시에 “이건 분명히 있다”고 판단한 결과에만 신뢰도를 높여 남기는 방법

3. **후처리**
   - 중복 박스 제거(NMS 등), 클래스 우선순위 적용

4. **성능 비교 및 분석**
   - **지표:** mAP(평균 정밀도), Recall, Precision 등
   - **대상:** 개별 모델, 각 앙상블별 결과
   - **분석:** 시간대, 날씨 등 다양한 조건별로 성능 차이 비교

### 5️⃣ **예상 효과 및 기대점**
- 세 모델의 **장점 결합**으로 다양한 환경/상황에서의 탐지 신뢰성 강화
- 악조건(야간, 혼잡 등)에서 단일 모델 대비 오탐·누락 감소
- 실시간 시스템 적용 시 안정성과 정확도 동시 개선

### 6️⃣ **프로젝트 활용 소프트웨어/플랫폼**
- **NVIDIA DeepStream SDK:** 실시간 멀티 모델 추론 및 앙상블 지원
- **PyTorch/TensorRT:** 사용자 정의 모델 실행, 후처리 커스터마이즈
- **Jupyter notebook:** 실험 및 결과 기록

### 7️⃣ **향후 발전 방향**
- 상황별 모델-가중치 동적 선택(Adaptive Ensemble)
- 추가 센서(RADAR 등) 정보 결합
- 실차/시뮬레이터 연동 및 실시간 프로토타입 제작

---

# 2. 💻 실제 코드로 적용해보기
[실제 돌려본 앙상블 코드](0731_앙상블.ipynb)

### 1️⃣ 계속 오류가 뜰 때, 지금까지 만든 파일을 삭제해주는 코드

<img width="849" height="325" alt="스크린샷 2025-07-31 14 06 48" src="https://github.com/user-attachments/assets/5a51ac0b-caa1-44fb-bfc9-39aaa2a98210" />

### 2️⃣ 필요한 패키지들을 설치하는 명령어

<img width="1158" height="318" alt="스크린샷 2025-07-31 14 08 14" src="https://github.com/user-attachments/assets/19f9badb-1e79-45d6-b728-b4fc5cfd105d" />

- `ensemble-boxes`: 객체 탐지 후처리에서 여러 모델의 박스 출력(바운딩 박스)를 앙상블하는 데 사용되는 라이브러리입니다. NMS(Non-maximum Suppression), Soft-NMS, Weighted Boxes Fusion(WBF) 등 다양한 앙상블 기법을 지원합니다.
- `ultralytics`: 대표적으로 YOLO(You Only Look Once) 시리즈 객체 탐지 모델을 제공하는 라이브러리입니다. 최신 YOLO 모델을 학습 및 추론하는 데 자주 사용됩니다.
- `opencv-python-headless==4.7.0.72`: OpenCV는 이미지 처리 및 컴퓨터 비전에 널리 쓰이는 라이브러리입니다. 여기서 "headless" 버전은 GUI(그래픽 사용자 인터페이스) 기능이 빠진 버전이라 서버 환경 등 그래픽 창이 필요 없는 곳에서 사용됩니다. 버전은 4.7.0.72로 지정돼 있습니다.
- `tqdm`: 파이썬에서 반복문 진행 상황을 한눈에 볼 수 있게 해주는 Progress Bar(진행바) 라이브러리입니다.
- 뒤의 `--quiet` 옵션은 설치 진행 시 출력되는 메시지를 최소화하여 콘솔을 깔끔하게 만듭니다.

### 3️⃣ 영상 불러오기

<img width="1158" height="235" alt="스크린샷 2025-07-31 14 46 34" src="https://github.com/user-attachments/assets/480b1f7f-4622-4a97-b535-7d23228495a7" />

### 4️⃣ YOLOv8n 모델의 가중치 파일(yolov8n.pt) 다운로드

<img width="813" height="235" alt="스크린샷 2025-07-31 14 57 21" src="https://github.com/user-attachments/assets/becc6891-06e7-4d0f-a3f1-b17d23cddc2b" />

- `os.path.exists(yolo_weight)`: yolov8n.pt 파일이 이미 있으면 다운로드를 건너뜁니다. 중복 다운로드 방지 목적입니다.
- `urllib.request.urlretrieve()`: 인터넷에서 지정한 URL의 파일을 로컬에 저장합니다.
- "https://github.com/ultralytics/assets/releases/download/v8.2.0/yolov8n.pt": Ultralytics에서 공식 배포하는 YOLOv8n 가중치 파일 주소입니다.

⁉️ **YOLOv8을 쓰고 YOLOv11을 안 쓰는 이유는?** (chat GPT에게 물어봤을 때 돌아온 답변이라 확실하지 않을 수도 있음
1. YOLOv8 최신 버전임
- 현재 시점(2025년 기준)에서 Ultralytics에서 공식적으로 지원하고 배포하는 최신 YOLO 버전은 YOLOv8입니다.
- 아직 YOLOv11 같은 버전은 공식 존재하지 않고, 아마도 상용 또는 비공식 버전일 수도 있거나 없을 가능성이 큽니다. 따라서 최신 모델을 쓰려면 YOLOv8을 선택하는 게 맞습니다.
2. 성능 및 사용 편의
- YOLOv8은 YOLO 시리즈 중 가장 최신이자 개선된 버전으로, 정확도와 속도 면에서 상당한 성능 향상을 이루었고, Ultralytics에서 파이썬 라이브러리 형태로 쉽게 쓸 수 있게 잘 만들어져 있습니다.
3. 공식 지원과 생태계
- Ultralytics가 직접 관리하고 배포하는 버전은 YOLOv8이며, 관련 문서나 튜토리얼, 커뮤니티도 YOLOv8에 맞춰져 있습니다.
- 오래된 버전(YOLOv4, YOLOv5 등)은 사용할 수 있지만, YOLOv11은 공식 자료가 아직 없기 때문에 실무 및 연구에 권장되지 않습니다.

### 5️⃣ numpy 패키지의 버전을 정확히 맞추기 위한 명령어

<img width="1148" height="324" alt="스크린샷 2025-07-31 15 03 06" src="https://github.com/user-attachments/assets/060e25b4-63b4-4cee-882e-cc5553a406df" />

- `!pip uninstall numpy -y`: 현재 설치된 numpy 패키지를 강제로 삭제합니다. `-y` 옵션 덕분에 "정말 삭제할까요?" 같은 추가 확인 과정 없이 자동으로 진행됩니다.
- `!pip install numpy==1.26.4`: numpy의 특정 버전(여기서는 1.26.4)을 새로 설치합니다. 버전을 명확하게 지정한 이유는,
- - 여러 라이브러리(특히 머신러닝, 딥러닝 패키지 등)가 서로 원하는 numpy 버전이 다를 수 있습니다.
- - 호환성 문제나 오류 예방을 위해 개발자가 검증한 버전의 numpy로 환경을 셋팅하려는 목적입니다.
 
### 6️⃣ YOLOv8n 모델과 클래스 정보를 불러오는 코드

<img width="794" height="245" alt="스크린샷 2025-07-31 15 09 23" src="https://github.com/user-attachments/assets/98aa7307-a6c6-468b-8bd3-072e1e204b27" />

- ultralytics의 YOLO 모듈을 불러옵니다. YOLO 객체 탐지 모델을 사용하기 위한 라이브러리입니다.
- `ensemble_boxes`의 `weighted_boxes_fusion` 함수는 여러 객체 탐지 결과(바운딩 박스)를 합치는 앙상블 기법 중 하나인 Weighted Boxes Fusion(WBF)을 수행합니다.
- cv2는 OpenCV 라이브러리로, 이미지나 영상 처리에 사용합니다.
- `numpy (np)`는 수치 계산 및 배열 처리를 위한 라이브러리입니다.
- os는 운영체제 관련 기능(파일 존재 여부 체크 등)을 위한 파이썬 내장 모듈입니다.
- "yolov8n.pt"라는 이름의 YOLOv8n 가중치 파일을 지정합니다.
- `YOLO(yolo_weight)`를 호출해 해당 가중치를 불러와 모델 객체를 만듭니다. 이 모델로 이미지나 영상에 대한 객체 탐지를 수행할 수 있게 됩니다.
- `yolo_model.names`는 모델이 학습된 클래스 이름들(예: 사람, 자동차, 강아지 등)의 리스트를 가져옵니다.
- 이 변수 `class_names`에 클래스명 리스트를 저장합니다.
- `class_to_id`는 클래스 이름을 키(key), 클래스 인덱스(숫자)를 값(value)으로 하는 딕셔너리를 만듭니다.
- - 예를 들어: {'person': 0, 'car': 1, ...}
- `id_to_class`는 `class_to_id`의 반대 딕셔너리로, 클래스 숫자 인덱스를 키로, 클래스명을 값으로 하는 딕셔너리입니다.
- - 예를 들어: {0: 'person', 1: 'car', ...}
 
### 7️⃣ 

<img width="922" height="620" alt="스크린샷 2025-07-31 15 17 12" src="https://github.com/user-attachments/assets/41deea2e-c9a3-4067-967e-ffa37aa53d2f" />


```
yolo_model = YOLO(yolo_weight)
if isinstance(yolo_model.names, dict):
    class_names = list(yolo_model.names.values())
else:
    class_names = yolo_model.names

class_to_id = {name: i for i, name in enumerate(class_names)}
id_to_class = {v: k for k, v in class_to_id.items()}
```
- `YOLO(yolo_weight)`로 YOLOv8 객체 탐지 모델을 불러옵니다.
- 모델의 클래스 이름(`yolo_model.names`)이 딕셔너리 형태일지 리스트 형태일지 모르므로, 둘 다 지원하도록 분기 처리합니다.
- 클래스 이름 → 숫자 인덱스(`class_to_id`), 숫자 인덱스 → 클래스 이름(`id_to_class`) 변환용 딕셔너리를 각각 만듭니다.  
  (탐지 결과 해석이나 시각화에서 편하게 쓰입니다.)

```
COLOR_TABLE = {
    'person': (255, 0, 0),        # 파랑
    'car': (0, 255, 0),           # 초록
    'truck': (0, 255, 255),       # 노랑
    'bus': (255, 255, 0),         # 하늘
    'motorcycle': (255, 0, 255),  # 보라
    'bicycle': (0, 128, 255),     # 주황
    'traffic light': (0, 0, 255), # 빨강
}
DEFAULT_COLOR = (200, 200, 200)   # 기타(회색)
```
- 클래스별로 객체(예: 사람, 자동차 등)에 맞는 RGB 색상을 지정합니다.  
  바운딩 박스를 이미지에 그릴 때 사용됩니다.
- COLOR_TABLE에 없는 클래스는 `DEFAULT_COLOR`(회색)로 박스가 그려집니다.


```
def real_peoplenet_inference(frame):
    # 본인 모델 추론 코드
    return []

def real_trafficnet_inference(frame):
    # 본인 모델 추론 코드
    return []
```
- 각 프레임(이미지)별로 객체 탐지를 수행할 함수 구조입니다.
- 현재는 예시(껍데기) 함수로, 실제 탐지 코드가 들어가야 하며, 현재는 처리 결과가 없는 빈 리스트만 반환합니다.
- 추후 PeopleNet/TrafficNet 등 다른 객체 탐지 모델 결과도 쉽게 붙일 수 있도록 형태를 맞춰둔 것입니다.
